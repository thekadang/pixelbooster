# 전체 시스템 구조

더카당 픽셀부스터의 전체 시스템 아키텍처를 설명합니다.

---

## 시스템 구성도

```
┌─────────────────────────────────────────────────────────────┐
│                     클라이언트 (Electron)                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │             Renderer Process (React)                   │ │
│  │  - UI 컴포넌트                                         │ │
│  │  - 사용자 인터랙션                                     │ │
│  │  - 상태 관리                                           │ │
│  └────────────────────────────────────────────────────────┘ │
│                            ↕ IPC                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │             Main Process (Node.js)                     │ │
│  │  - ImageProcessor (Sharp)                              │ │
│  │  - FileSystem 접근                                     │ │
│  │  - LogManager (Excel)                                  │ │
│  │  - BackupManager                                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                            ↕                                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │             Services                                   │ │
│  │  - SubscriptionManager                                 │ │
│  │  - DeviceManager                                       │ │
│  │  - AuthService                                         │ │
│  │  - I18nManager                                         │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            ↕ HTTPS
┌─────────────────────────────────────────────────────────────┐
│                    Supabase (BaaS)                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Auth (JWT)          PostgreSQL DB      Edge Functions │ │
│  │  - 회원가입/로그인    - 사용자 정보     - 커스텀 로직  │ │
│  │  - 토큰 관리         - 구독 정보       - 기기 인증     │ │
│  │                      - 기기 정보       - 수수료 계산   │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                            ↕ HTTPS
┌─────────────────────────────────────────────────────────────┐
│               관리자 대시보드 (Web)                         │
│  - 사용자 관리                                              │
│  - 수익 통계                                                │
│  - 어필리에이트 관리                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 프로젝트 폴더 구조

```
thekadang_pixelbooster/
├── docs/                           # 📚 문서 시스템
│   ├── architecture/               # 아키텍처 문서
│   ├── features/                   # 기능 상세 문서
│   ├── development/                # 개발 가이드
│   ├── api/                        # API 명세
│   └── deployment/                 # 배포 가이드
│
├── client/                         # 💻 데스크톱 앱 (Electron)
│   ├── src/
│   │   ├── main/                  # Main Process
│   │   │   ├── index.ts           # Electron 엔트리 포인트
│   │   │   ├── core/              # 핵심 로직
│   │   │   │   ├── ImageProcessor.ts
│   │   │   │   ├── LogManager.ts
│   │   │   │   └── BackupManager.ts
│   │   │   └── services/          # 비즈니스 로직
│   │   │       ├── SubscriptionManager.ts
│   │   │       ├── DeviceManager.ts
│   │   │       └── AuthService.ts
│   │   │
│   │   ├── renderer/              # Renderer Process (React)
│   │   │   ├── App.tsx
│   │   │   ├── components/        # UI 컴포넌트
│   │   │   │   ├── HeaderBar.tsx
│   │   │   │   ├── DropZone.tsx
│   │   │   │   ├── SettingsPanel.tsx
│   │   │   │   └── ProgressBar.tsx
│   │   │   ├── pages/             # 페이지
│   │   │   │   ├── MainPage.tsx
│   │   │   │   └── LoginPage.tsx
│   │   │   └── hooks/             # 커스텀 훅
│   │   │
│   │   ├── config/                # 설정
│   │   │   └── app-config.ts
│   │   │
│   │   ├── locales/               # 다국어 리소스
│   │   │   ├── ko.json
│   │   │   └── en.json
│   │   │
│   │   └── types/                 # 타입 정의
│   │       └── index.ts
│   │
│   ├── package.json
│   └── electron-builder.config.js
│
├── server/                         # 🌐 서버 (Supabase)
│   └── supabase/
│       ├── functions/             # Edge Functions
│       │   ├── login-with-device-check/
│       │   ├── check-subscription/
│       │   ├── register-device/
│       │   ├── track-referral/
│       │   └── process-commission/
│       │
│       └── migrations/            # DB 마이그레이션
│           ├── 20251110_initial_schema.sql
│           └── seed.sql
│
├── admin/                          # 👨‍💼 관리자 대시보드 (Web)
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Dashboard.tsx
│   │   │   ├── UserManagement.tsx
│   │   │   ├── RevenueManagement.tsx
│   │   │   └── AffiliateManagement.tsx
│   │   ├── components/
│   │   └── services/
│   └── package.json
│
├── shared/                         # 🔄 공통 코드
│   ├── types/                     # 공통 타입
│   │   ├── user.ts
│   │   ├── subscription.ts
│   │   └── device.ts
│   └── utils/                     # 공통 유틸리티
│
├── CLAUDE.md                       # 📌 문서 허브
├── task.md                         # 📋 진행 상황
├── 기획서.md                       # 📄 기획 문서
├── 기술적계획.md                   # 📄 기술 계획
├── .gitignore
├── README.md
└── package.json                    # 루트 workspace 설정
```

---

## 핵심 컴포넌트 설명

### 클라이언트 (Electron)

#### Main Process
- **ImageProcessor**: Sharp 라이브러리를 사용한 이미지 변환
- **LogManager**: ExcelJS를 사용한 작업 로그 생성
- **BackupManager**: 원본 파일 백업 처리
- **SubscriptionManager**: 구독 등급 관리 및 기능 제한
- **DeviceManager**: 기기 고유 ID 생성 및 인증
- **AuthService**: Supabase 인증 처리

#### Renderer Process (React)
- **UI 컴포넌트**: 사용자 인터페이스
- **상태 관리**: 앱 상태 및 진행 상황
- **IPC 통신**: Main Process와의 데이터 교환

### 서버 (Supabase)

#### Auth
- JWT 기반 인증
- 이메일/비밀번호 로그인

#### PostgreSQL DB
- 사용자 정보
- 구독 정보
- 기기 등록 정보
- 어필리에이트 데이터

#### Edge Functions
- 커스텀 비즈니스 로직
- 기기 인증 처리
- 수수료 계산

### 관리자 대시보드 (Web)
- React 기반 웹 애플리케이션
- 사용자/수익/어필리에이트 관리

---

## 데이터 흐름

### 이미지 변환 프로세스
```
1. 사용자 파일 선택 (Renderer)
   ↓
2. 구독 등급 확인 (SubscriptionManager)
   ↓
3. IPC를 통해 Main Process로 전달
   ↓
4. ImageProcessor로 배치 처리
   ↓
5. 진행 상태를 Renderer로 전송
   ↓
6. LogManager로 기록 (Basic 이상)
   ↓
7. 완료 알림
```

### 인증 프로세스
```
1. 사용자 로그인 시도 (Renderer)
   ↓
2. 기기 ID 생성/조회 (DeviceManager)
   ↓
3. Supabase Edge Function 호출
   ↓
4. 기기 등록 수 확인
   ↓
5. 인증 성공 시 토큰 저장
   ↓
6. 구독 정보 조회 (SubscriptionManager)
   ↓
7. 메인 화면 표시
```

---

## 기술 스택

### 클라이언트
- **프레임워크**: Electron 28+
- **UI**: React 18+ with TypeScript
- **이미지 처리**: Sharp
- **Excel**: ExcelJS
- **다국어**: i18next
- **상태 관리**: React Context API
- **스타일링**: Tailwind CSS / CSS Modules

### 서버
- **BaaS**: Supabase
- **데이터베이스**: PostgreSQL
- **인증**: Supabase Auth (JWT)
- **서버리스**: Deno (Edge Functions)

### 관리자 대시보드
- **프레임워크**: React 18+
- **라우팅**: React Router
- **UI 라이브러리**: Material-UI / Ant Design
- **차트**: Recharts

---

## 보안 고려사항

1. **통신 암호화**: 모든 API 통신은 HTTPS
2. **토큰 관리**: JWT 토큰을 안전하게 저장
3. **RLS**: Supabase Row Level Security 활성화
4. **기기 인증**: 등급별 기기 등록 수 제한
5. **환경 변수**: API 키는 환경 변수로 관리

---

## 확장성 계획

1. **초기**: Supabase 무료 플랜
2. **성장**: Supabase 유료 플랜으로 업그레이드
3. **대규모**: AWS/GCP로 이전 (PostgreSQL 표준 사용으로 쉬운 마이그레이션)

---

**참고 문서**:
- [데이터베이스 설계](database-schema.md)
- [보안 아키텍처](security-architecture.md)
- [API 명세](../api/api-specification.md)
